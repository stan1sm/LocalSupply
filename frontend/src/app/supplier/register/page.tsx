/**
 * @module routes/supplier/register
 * Next.js route entry — renders SupplierRegistrationPage.
 */

import SupplierRegistrationPage from '../../../features/pages/supplier/SupplierRegistrationPage'

/** Next.js page component — delegates rendering to SupplierRegistrationPage. */
export default function Page() {
  return <SupplierRegistrationPage />
}
